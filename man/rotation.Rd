% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotation.R
\name{rotation}
\alias{rotation}
\title{Rotation test for HDANOVA}
\usage{
rotation(
  object,
  rotate = 1000,
  unique.digits = 12,
  unique.frac = 0.95,
  block.type = c("denominator", "global")
)
}
\arguments{
\item{object}{A \code{hdanova} object.}

\item{rotate}{Number of random rotations to perform (default = 1000).}

\item{unique.digits}{Number of digits used when rounding rotation SSQ values before
checking uniqueness (default = 12). Set to \code{NULL} to disable this warning.}

\item{unique.frac}{Minimum fraction of unique rounded SSQ values required to avoid
warning (default = 0.95). Set to \code{NULL} to disable this warning.}

\item{block.type}{Rotation blocking strategy. \code{"denominator"} (default)
rotates within denominator-compatible exchangeable blocks. \code{"global"}
rotates across all observations.}
}
\value{
An updated \code{hdanova} object with rotation-test results stored in
\code{object$permute} for compatibility with existing summary and plotting tools.
}
\description{
Rotation testing for HDANOVA. This function performs
random orthogonal rotations of exchangeable residual units for each approved
effect and adds the resulting null distributions to the \code{hdanova} object.
}
\examples{
# Load candies data
data(candies)

# Basic HDANOVA model with two factors
mod <- hdanova(assessment ~ candy + assessor, data=candies)

# Rotation test
modRot <- rotation(mod)
summary(modRot)

# Plot null distribution for "candy" effect
rotationplot(modRot, factor="candy")

}
\seealso{
Base fitting: \code{\link{hdanova}}.
Permutation alternative: \code{\link{permutation}}.
Plot helper: \code{\link{rotationplot}}.
}
